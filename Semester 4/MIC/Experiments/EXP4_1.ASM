DATA SEGMENT
NUM1 DB -05H
NUM2 DB 03H
PROD DW ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
MOV AX, DATA
MOV DS, AX

MOV AL, NUM1
MOV BL, NUM2
IMUL BL
MOV PROD, AX

MOV AH, 4CH
INT 21H
CODE ENDS
END START
