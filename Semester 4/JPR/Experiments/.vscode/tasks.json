{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compileActiveJavaFile",
      "type": "shell",
      "command": "javac",
      "args": [
        "${file}",
        "-d",
        "${fileDirname}"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Compiles the active Java file and places the .class file in the same folder."
    }
  ]
}
