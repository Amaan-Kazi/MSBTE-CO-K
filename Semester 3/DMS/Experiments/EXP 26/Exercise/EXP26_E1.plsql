CREATE TABLE EMP (
    empid NUMBER,
    salary NUMBER
);

CREATE OR REPLACE TRIGGER onSal
AFTER INSERT OR UPDATE
OF salary
ON EMP
FOR EACH ROW
WHEN (NEW.salary < 5000)
BEGIN
    DBMS_OUTPUT.PUT_LINE('Salary < 5000');
END;

INSERT INTO EMP
VALUES (1, 2000);

COMMIT;
